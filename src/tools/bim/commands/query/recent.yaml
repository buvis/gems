sort:
  - field: date
    order: desc

output:
  limit: 50

columns:
  - field: title
  - field: type
  - field: date
  - field: tags
  - field: file_path

dashboard:
  title: Recent

actions:
  - name: delete
    label: Delete
    handler: delete
    confirm: "Permanently delete this note?"
  - name: format
    label: Format
    handler: format
  - name: open
    label: Open
    handler: open
