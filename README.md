# BUVIS Refractable Interface eXtensions

Each BRIX is an interface that can refract — meaning it takes one internal implementation and presents it through multiple surfaces without changing the core. The same data/logic transforms in presentation (like light changing color through a prism) but not in substance.
